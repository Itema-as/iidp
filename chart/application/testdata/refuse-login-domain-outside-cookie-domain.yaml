application:
  name: shop
environment: prod
platform:
  baseDomain: app.itma.no
  loginCookieDomain: itma.no
kind: web-service
image:
  repository: ghcr.io/itema-as/shop
  tag: 1.4.2
size: small
login:
  enabled: true
domains:
  # Inside the cookie domain: allowed.
  - x.itma.no
  # Outside it: refused, and named.
  - shop.example.com
  # Ends with the cookie domain's characters but is not under it.
  - notitma.no
