application:
  name: brochure
environment: prod
platform:
  baseDomain: app.itma.no
kind: static-site
image:
  repository: ghcr.io/itema-as/brochure
  tag: 1.0.0
size: small
tasks:
  - name: rebuild
    schedule: "0 3 * * *"
    command: echo rebuild
