application:
  name: shop
environment: prod
platform:
  baseDomain: app.itma.no
  # Not a domain containing baseDomain: the Platform address itself would
  # never get the cookie.
  loginCookieDomain: example.com
kind: web-service
image:
  repository: ghcr.io/itema-as/shop
  tag: 1.4.2
size: small
login:
  enabled: true
