apiVersion: v2
name: application
description: The generic chart every Application on Itema's Platform is an instance of. One values file per Environment is the Application's whole definition.
type: application
# The release workflow overrides this with the iidp tag's version when it
# packages and pushes the chart, so the chart and the CLI share one version.
version: 0.1.0
