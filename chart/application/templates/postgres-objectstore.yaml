{{- include "application.validate" . -}}
{{- if and (include "application.released" .) (include "application.postgres.enabled" .) }}
apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: {{ include "application.postgres.cluster" . }}
  labels:
    {{- include "application.labels" . | nindent 4 }}
  annotations:
    # With the Cluster, which references it from its first reconcile.
    argocd.argoproj.io/sync-wave: "-2"
    # Kept with the Cluster: a kept database whose ObjectStore was pruned
    # could no longer archive WAL, which would pile up on its volume.
    {{- include "application.postgres.keepOnPrune" . | nindent 4 }}
spec:
  configuration:
    destinationPath: {{ include "application.postgres.backupPath" . | quote }}
    endpointURL: {{ include "application.postgres.objectStorageEndpoint" . | quote }}
    # The Object Storage keys, in a Secret the Platform puts into every
    # Environment's namespace. The chart never sees the values.
    s3Credentials:
      accessKeyId:
        name: {{ .Values.platform.backupsCredentialsSecret | toString | quote }}
        key: ACCESS_KEY_ID
      secretAccessKey:
        name: {{ .Values.platform.backupsCredentialsSecret | toString | quote }}
        key: ACCESS_SECRET_KEY
    wal:
      compression: gzip
    data:
      compression: gzip
  # Backups and WAL older than this are deleted from the bucket.
  retentionPolicy: {{ .Values.postgres.backupRetention | toString | quote }}
  # The plugin's sidecar in the database Pod, which runs barman-cloud for
  # every WAL segment and base backup. It has no resources of its own
  # unless given some here, and the guardrails want limits on every
  # container (bootstrap/components/guardrails). A small request, since the
  # node and the kind runner are near their CPU request ceiling, and room
  # above it for a compressed base backup's upload.
  instanceSidecarConfiguration:
    resources:
      requests:
        cpu: "10m"
        memory: "64Mi"
      limits:
        cpu: "500m"
        memory: "512Mi"
{{- end }}
