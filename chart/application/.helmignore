# Kept out of the packaged chart: the tests and their fixtures live next to
# the templates so there is one test runner, but they are not part of what
# the Platform repository pulls.
chart_test.go
testdata/
.DS_Store
