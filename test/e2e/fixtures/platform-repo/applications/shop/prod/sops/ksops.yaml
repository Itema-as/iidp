apiVersion: viaduct.ai/v1
kind: ksops
metadata:
    name: shop-secrets
    annotations:
        config.kubernetes.io/function: |
            exec:
              path: ksops
files:
    - backups-credentials.enc.yaml
