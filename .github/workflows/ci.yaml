name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  go:
    name: Go
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - name: gofmt
        run: |
          unformatted="$(gofmt -l .)"
          if [ -n "$unformatted" ]; then
            echo "files need gofmt:"
            echo "$unformatted"
            exit 1
          fi
      - name: go vet
        run: go vet ./...
      - name: go test
        run: go test ./...

  # The chart tests skip themselves when helm or kubeconform is missing, so
  # the Go job above stays tool-free and this job is where they really run.
  chart:
    name: Chart
    runs-on: ubuntu-latest
    env:
      KUBECONFORM_VERSION: v0.8.0
      KUBECONFORM_SHA256: 9bc2bffbf71f261128533edaf912153948b7ff238f9a531ae6d34466ec287883
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - uses: azure/setup-helm@v5
        with:
          version: v4.3.0
      - name: Install kubeconform
        run: |
          curl -sSfL --retry 5 --retry-delay 5 --retry-all-errors -o kubeconform.tar.gz \
            "https://github.com/yannh/kubeconform/releases/download/${KUBECONFORM_VERSION}/kubeconform-linux-amd64.tar.gz"
          echo "${KUBECONFORM_SHA256}  kubeconform.tar.gz" | sha256sum -c -
          tar -xzf kubeconform.tar.gz kubeconform
          sudo install -m 0755 kubeconform /usr/local/bin/kubeconform
          rm kubeconform kubeconform.tar.gz
          kubeconform -v
      # The chart has required values, so a bare lint only warns. Linting
      # with every rendering fixture under --strict turns warnings into
      # errors. Fixtures named refuse-* exist to be refused by the templates
      # and are covered by the tests instead.
      - name: helm lint
        run: |
          for values in chart/application/testdata/*.yaml; do
            case "$(basename "$values")" in refuse-*) continue ;; esac
            helm lint --strict chart/application --values "$values"
          done
      - name: go test
        # The chart tests skip when a tool is missing; here that must fail.
        env:
          IIDP_REQUIRE_CHART_TOOLS: "1"
        run: go test -v ./chart/...

  # Renders each built-in Create template with cmd/renderfixture, builds its
  # Dockerfile, and runs it the way the Platform does (#90): the
  # application chart with runAsNonRoot: true, in a kind namespace that
  # enforces Pod Security's restricted level. test/templates/run-restricted.sh
  # fails on any admission warning, a Pod that does not become Ready, a
  # container running as root, or anything but a 200 on / at the template's
  # port (3000 for Next.js, 8080 for the unprivileged nginx of Vite React).
  # "other" is excluded: its stub Dockerfile is not meant to build.
  templates:
    name: Templates
    runs-on: ubuntu-latest
    strategy:
      matrix:
        framework: [nextjs, vite-react]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
      - uses: azure/setup-helm@v5
        with:
          version: v4.3.0
      - uses: helm/kind-action@v1
        with:
          version: v0.33.0
          install_only: true
      - name: Render the template
        run: go run ./cmd/renderfixture ${{ matrix.framework }} demoapp fixture
      - name: Build the image
        run: docker build -t iidp-templates.local/${{ matrix.framework }}:ci fixture
      - name: Run it under Pod Security restricted and curl /
        run: test/templates/run-restricted.sh ${{ matrix.framework }} iidp-templates.local/${{ matrix.framework }}:ci
      - name: npx and the npm cache work as the runtime user
        if: matrix.framework == 'nextjs'
        run: docker run --rm iidp-templates.local/nextjs:ci sh -c 'npx --version && npm cache verify'

  release-config:
    name: GoReleaser check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: goreleaser/goreleaser-action@v7
        with:
          distribution: goreleaser
          version: "~> v2"
          args: check

  # shellcheck and bash ship on the runner image; test/wizard/run.sh runs the
  # wizard's wrapper functions with IIDP_WIZARD_FAKE=1 (no network, no real
  # tofu/gh/sops/az/ssh calls) and the full script with --dry-run, so this
  # needs no Hetzner, Cloudflare, GitHub App or Entra credentials.
  wizard:
    name: Wizard
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: shellcheck
        run: shellcheck scripts/bootstrap-wizard.sh test/wizard/run.sh
      - name: fake run
        run: bash test/wizard/run.sh
